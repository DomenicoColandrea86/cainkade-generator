language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-cainkade' ]; then cd .. && eval "mv $currentfolder generator-cainkade" && cd generator-cainkade; fi

